fn main() {
    // Stub GUI entrypoint. This binary only builds when the `gui` feature is enabled.
    // Keeping it minimal so CLI can be developed independently.
    eprintln!("meta-gui: GUI is not implemented yet. Enable with: cargo run --no-default-features --features gui --bin meta-gui");
}
